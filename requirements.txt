# ScreenPrompt dependencies
# Core: Python 3.10+ with tkinter (included in standard library)

# Optional: Global hotkeys support
keyboard>=0.13.5

# Development dependencies (for building)
# pip install -r requirements-dev.txt
